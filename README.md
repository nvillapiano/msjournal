# ms-journal-agent (Phase 1 Restore)

Local-first MS journaling agent with:

- Node 20, Express
- Vanilla JS frontend (no Tailwind)
- Markdown + YAML frontmatter storage
- Optional local LLM via Ollama, OpenAI fallback
- Git-backed history (auto-commits)
